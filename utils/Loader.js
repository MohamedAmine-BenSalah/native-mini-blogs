// todo : add loader that stops once the data is fetched
// loader functions 
export function showLoader() {
    // get loader container and display will be shown
    $('#loader').show();
  }
  
export function hideLoader() {
    // get loader container and display will be none
    $('#loader').hide();
  }